# render.yaml
services:
  - type: web
    name: codecraft-frontend
    env: static
    buildCommand: npm ci --no-audit --no-fund && npm run build
    staticPublishPath: ./dist
    envVars:
      - key: NODE_ENV
        value: production
